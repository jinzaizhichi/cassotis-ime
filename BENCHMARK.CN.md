# Cassotis 语料基准测试

Cassotis 对外发布四套固定语料基准：长句基准测试-16300、短词上下文基准测试-65000、一键补全上下文基准测试-12831，以及长句一键补全基准测试-16300。它们用于持续跟踪不同版本的解码质量与引擎性能，把发布质量变成可复现的指标，而不是只依赖少量手工测试案例。

中等输入一键补全基准测试-10840 和文档复制式补全顺序回放基准测试-386 保留为内部专项测试，不作为对外版本指标。`run_document_context_benchmark.ps1` 也是内部诊断工具；`run_changju_latency_16300.ps1` 只是长句基准的延迟测试入口，`run_completion_benchmark_all.ps1` 只是补全测试的聚合入口，它们都不构成新的基准。

## 共同语料来源

公开基准与内部专项测试的语料均来自开发者自己的小说著作 [**《永恒的舞动》**](https://www.qidian.com/book/1037259117/)（英文名 **Elegance in Timelessness**）。

长句基准固定收录 16,300 条合格句子，短词基准固定收录 65,000 个短词出现位置；短词一键补全基准从同一套短词案例中构造 12,831 个逐音节补全机会。中等输入与长句一键补全基准复用长句语料，分别构造 10,840 个中段补全机会和 16,300 个近句尾补全机会。文档复制式顺序回放基准固定收录 386 个目标续写已经在同一文档前文出现过的机会。基准案例与对应模型的训练数据保持隔离。

## 共同准确率等价规则

以下规则适用于长句和短词上下文两套基准中基于可见候选统计的准确率指标，包括 `Top1`、`Top2`、其他已报告的 `TopN`，以及短词基准的 `Contested` 指标：

- 仅当“他”和“她”出现在相同字符位置时，二者才视为等价，因为基准拼音查询无法区分它们。
- “它”、其他同音字、字符缺失或增加，以及任何其他文本差异仍按不同结果判定。
- 该规则只改变离线基准的通过/失败判定，不改写候选文本，也不影响候选生成、排序、延迟或用户词库行为。
- raw pool 和 Oracle 召回评分继续采用严格逐字匹配，确保目标标签不会参与并影响搜索过程，因此不能与采用等价规则的可见 `TopN` 指标直接比较。

这一等价规则从 `v1.11.0` 的基准测试结果开始采用。`v1.10.0` 及更早版本采用严格逐字匹配；如需与 `v1.11.0` 及后续版本直接比较，应先按新口径重新评分。

## 长句基准测试-16300

### 语料规模与案例构造

长句基准测试按固定顺序收录从小说中抽取的 16,300 条合格句子，案例按以下规则生成：

- 按标点符号和换行分割小说文本。
- 忽略包含英文字母的句子。
- 忽略短于配置中最小中文字符数的句子。
- 使用基准测试的反向拼音构建器，将完整句子转换为拼音。
- 将完整拼音查询交给待测试版本的引擎和词库生成候选。

### 准确率评分

- 第一条完整候选按上述共同评分规则与原句匹配时，计为 `Top1` 通过。
- 前两条完整候选中任意一条按上述共同评分规则与原句匹配时，计为 `Top2` 通过。

### 准确率与延迟模式

- 准确率测试使用确定性工作量模式：关闭正常搜索路径的墙钟截断，只保留 beam、状态、边、候选数等固定工作量上限，使机器负载变化不影响正常候选生成。
- 延迟测试使用生产模式：固定工作量上限是主要边界，同时保留较宽的墙钟紧急上限，防止错误长拼音或低性能设备出现不可接受的卡顿。
- 两种模式都会加载与 Host 实际部署相同的长句 Transformer 重排器；运行文件缺失或不完整时直接报错，不再静默退回旧路径。显式关闭只用于明确标注的诊断测试。
- 两种数据分别运行。八分片脚本用于提高准确率测试效率，不用于延迟测量。

### 延迟测试口径

长句延迟数据衡量引擎层的完整查询解码耗时：

- 在单个测试进程中，按照固定语料顺序串行处理全部 16,300 条案例。
- 使用待测试版本对应的简体基础词库快照，默认关闭用户词库。
- 每条案例开始前重置引擎组合状态；整轮测试复用同一个词库连接及其运行时缓存。
- 一次性设置完整拼音查询，然后生成并读取候选列表。
- 从设置完整查询之前开始计时，到候选读取完成时停止。
- 不计入进程启动、词库打开、反向拼音转换、报告写入、TSF 集成、候选窗口渲染、真实按键及按键间隔。

## 短词上下文基准测试-65000

### 语料规模与案例构造

短词基准测试用于衡量带前文的按词输入，并与长句基准使用同一篇小说文本作为来源：

- 对每条句子进行确定性分词，并整理为符合日常按词输入习惯的二至四字单元。
- 只保留经过人工允许词表审查的词汇单元，同时排除小说专有人名，避免测试退化为对作品特定名称的记忆。
- 每个合格词汇的实际出现位置构成一条案例，并保留用户输入该词之前已经上屏的句子前文。
- 独立将目标词转换为拼音；对多音词使用经过复核的读音覆盖。
- 按原始语料顺序保留案例，并冻结前 65,000 个合格出现位置，形成短词上下文基准测试-65000。
- 使用待测试版本对应的简体基础词库快照，默认关闭用户词库排序。

固定测试集包含 55,712 条具有可用左侧上下文的案例，以及 9,288 条位于句首、没有左侧上下文的案例。

### 准确率与歧义子集评分

- 第一条精确候选按上述共同评分规则与目标词匹配时，计为 `Top1` 通过。
- 前两条精确候选中任意一条按上述共同评分规则与目标词匹配时，计为 `Top2` 通过。
- `Contested` 是同一拼音查询在语料中对应至少两个目标词的 11,728 条歧义案例。`Contested Top1` 和 `Contested Top2` 单独衡量左侧上下文对消歧最有价值的部分。
- 短词结果采用启用上下文的基准测试。

### 延迟测试口径

短词延迟数据衡量启用上下文轨道的引擎候选查询耗时：

- 在单个测试进程中，按照固定语料顺序串行处理全部 65,000 条案例。
- 每次查询前重置引擎，整轮测试复用同一个词库连接及其运行时缓存。
- 在开始计时前设置已经上屏的句子前文；随后一次性设置目标词的完整拼音，到候选读取完成时停止计时。
- 不计入语料分词、拼音生成、进程启动、词库打开、报告写入、TSF 集成、候选窗口渲染、真实按键及按键间隔。

## 一键补全上下文基准测试-12831

### 案例构造与评分

一键补全基准复用短词上下文基准测试-65000 的固定案例和左侧前文，专门衡量目标词尚未输入完成时的一键续写能力：

- 只测试至少包含三个完整拼音音节的目标词。
- 从已输入两个音节开始逐音节推进，直到完整拼音的前一个音节；每个中间状态构成一次补全机会。
- 例如目标为“往常一样”，会分别用 `wangchang` 和 `wangchangyi` 查询同一个完整目标。
- 完整 65,000 条源案例固定生成 12,831 个补全机会，因此正式名称为“一键补全上下文基准测试-12831”。
- 评测使用对应版本的简体基础词库快照，关闭用户词库，并启用左侧上下文。
- 每个机会只读取界面实际会显示的唯一补全方案。该方案与语料目标严格一致时计为命中，不采用“他/她”等价规则。

基准记录四项容易解释和跨版本比较的指标：

- `补全命中率`：正确补全数除以全部 12,831 个补全机会，是主要质量指标。
- `平均节省按键`：每次正确补全相对继续输入完整拼音所节省的平均按键数，已经扣除一次接受补全的按键。
- `逐键稳定率`：继续输入后原方案仍然匹配时，补全内容保持不变的比例。
- `P95`：95% 的补全查询在该毫秒数以内完成。

由于语料只给出一个目标答案，其他合理但不同的补全仍会按未命中处理。

### 延迟测试口径

延迟只统计词库查询、上下文/语言模型评分、补全选择和迟滞处理，不包含进程及词库冷启动、TSF/host 通信、候选窗口绘制、实际按键间隔或接受补全后的学习写入。每条源案例开始前重置引擎状态，同一目标的相邻前缀连续处理，整轮测试复用词库连接和运行时缓存。

## 内部专项：中等输入一键补全基准测试-10840

### 案例构造与评分

这套基准填补短词补全与近句尾长句补全之间的空白，复用固定的 16,300 条长句及其完整拼音：

- 每条案例保留最后两个完整拼音音节尚未输入，只测试已经输入 5 至 12 个完整音节的状态。
- 符合条件的长句固定产生 10,840 个补全机会。
- 接入与 Host 相同的静态补全和受约束局部续写模型，但只读取界面最终会显示的唯一方案。
- 当提示严格扩展已输入的目标前缀，并且提示文本仍是参考句的前缀时计为命中。
- 使用确定性基准模式、简体基础词库快照和空用户词库；基准候选池与模型训练语料保持隔离。

内部报告记录五项指标：

- `补全命中率`：正确局部续写数除以全部 10,840 个机会。
- `提示精度`：正确续写数除以实际显示提示的机会数。
- `平均节省按键`：每次正确命中的平均净节省按键数，已扣除接受补全所需的一次按键。
- `逐键稳定率`：继续输入且上一方案仍兼容时，提示保持不变的比例。
- `P95`：95% 的可见补全查询在该毫秒数以内完成。

语料只提供一个参考续写，因此其他自然但不同的表达仍按未命中处理。详细诊断报告另外保留词库、强转移、后缀索引和局部模型各来源的 Top-32 候选及 Oracle 排名，用于区分召回与排序问题。

### 延迟测试口径

正式计分前加载并预热词库和模型。延迟包含中等长度拼音解码、各补全来源查询、统一选择与实际应用的局部模型复核；不包含进程和模型冷启动、报告写入、TSF/Host IPC、候选窗绘制、真实按键间隔和学习写入。诊断候选池在延迟采样结束后生成，不计入可见延迟。

## 长句一键补全基准测试-16300

### 案例构造与评分

这套基准直接衡量一键补全能否续写已经部分解码的长句，不再用无关的长句候选排序成绩间接判断补全质量：

- 复用长句基准测试中固定的 16,300 条案例及其完整拼音。
- 固定留下最后四个完整拼音音节尚未输入，同时至少保留前四个音节作为候选窗中的组合前缀。
- 使用确定性工作量模式解码该前缀，并接入 Host 实际使用的长句 Transformer 重排器。
- 当静态补全层请求异步复核时，调用与 Host 相同的受约束局部续写模型，执行相同的置信度、超时和 exact 路径校验，然后只读取界面稳定后实际显示的唯一补全方案。
- 当显示结果严格扩展目标输入前缀，并且显示出的完整文本仍是参考句前缀时，计为一次局部续写命中。补全可以在接下来的一个至三个局部词后结束，不要求一次补到整句末尾。
- 关闭用户词库和外部文档前文，使用待测版本对应的简体基础词库快照。
- 在计分查询前额外查询前一个音节边界，用于判断继续输入时兼容的补全方案是否保持稳定。

公开报告采用四项适合在现行口径下直接跨版本比较的指标：

- `局部续写命中率`：正确局部续写数除以全部 16,300 个补全机会。
- `提示覆盖率`：实际显示了任一补全方案的机会数除以全部机会数。
- `总节省按键`：所有正确局部续写命中的净节省按键总和，并为每次接受补全扣除一个按键。
- `P95`：95% 的可见补全查询在该毫秒数以内完成。

详细报告另外保留单次命中平均节省按键、逐键稳定率、错误提示数、整句严格命中和内部候选池 Oracle 排名，用于进一步归因。逐键稳定率的可比较分母取决于各版本实际产生的提示，且可能很小，因此不再作为公开横向比较的主要指标。由于语料只有一个参考答案，措辞不同但同样合理的续写仍按未命中处理。

### 延迟测试口径

正式计分前会完成词库和两套运行时模型的加载与预热。延迟从设置本次计分拼音前缀之前开始，包括长句解码、exact/转移补全查询、语言模型评分、迟滞处理，以及被接受并实际更新界面的局部模型复核。模型弃权或超时的工作在实际 Host 中异步运行，不会延迟静态结果的显示，因此不计入可见延迟；进程和模型冷启动、前一个音节的稳定性探测、报告写入、TSF 到 Host 的 IPC、窗口绘制、真实按键间隔和学习写入也不计入。每条长句开始前重置引擎状态，整轮测试复用词库连接、模型会话和运行时缓存。

## 内部专项：文档复制式补全顺序回放基准测试-386

### 案例构造与评分

这套基准用于衡量文档内复制式补全，并严格排除未来文本泄漏：

- 按小说原始顺序回放固定语料，每条案例只能读取被测句子之前已经出现的内容。
- 文档快照最多保留光标前 1,024 个字符，与正式运行环境的快照上限一致。
- 固定选择 386 个机会；相同的局部锚点和目标续写必须已经在可用前文中出现过。
- 参考续写必须由一至三个基础词库 exact 词组成。用户词以及只在文档后文出现的内容不能产生案例或提示。
- 将历史快照、文档标识、当前拼音前缀和普通引擎候选交给与 Host 相同的文档复制式选择路径。
- 这套专用分层基准关闭异步局部补全运行时；当文档复制证据不足而弃权时，仍保留现有静态补全作为后备。

内部报告记录四项指标：

- `文档复制命中率`：严格匹配参考答案的文档复制提示数除以全部 386 个机会。
- `提示精度`：严格匹配参考答案的提示数除以实际显示的全部提示数，其中包括后备提示。
- `总节省按键`：所有严格命中的净节省按键总和，并为每次接受补全扣除一个按键。
- `P95`：95% 的顺序回放查询在该毫秒数以内完成。

语料只提供一个参考续写，因此其他自然但不同的表达仍按未命中处理。这一严格规则保证跨版本结果可重复比较；详细 TSV 另行保留提示来源、锚点、exact 后缀路径、分数和出现次数，供进一步归因。

### 延迟测试口径

延迟包含普通候选生成、文档后缀查询、exact 路径校验、评分和同步静态后备；不包含案例准备、进程与词库冷启动、TSF 到 Host 的 IPC、候选窗口绘制、真实按键间隔、异步神经后备和反馈写入。每个机会都从冻结的历史快照开始，因此此前案例中的基准决策不会影响后续案例。

## 延迟统计

延迟列统一使用毫秒：

- `Mean`：全部查询解码耗时的算术平均值。
- `P50`：采用最近秩法计算的中位数，即 50% 的查询在该时间以内完成。
- `P95`：采用最近秩法计算的第 95 百分位数，即 95% 的查询在该时间以内完成。
- `Max`：本轮测试中单次查询耗时的最大值。

这些数据用于衡量完整查询的引擎性能和长尾开销，不是逐键输入到候选窗口显示的延迟，也不能表述为端到端打字延迟。只有在测试机器、操作系统、电源模式、Release 构建设置、语料顺序和词库快照保持一致时，延迟对比才具有意义。

## 结果发布

四项公开基准的具体版本结果发布在 [README.zh-Hans.md](README.zh-Hans.md) 中；中等输入和文档回放结果仅用于内部研发与回归，不列入公开版本表格。短词一键补全基准从 `v1.15.0` 开始记录。`v1.17.0` 及以前的长句补全数据采用旧的整句严格命中口径；局部续写口径从下一个正式测试版本开始使用，且不追溯重算旧版本。本文档定义共同语料来源、案例构造、准确率评分和延迟测试口径。

## 说明

基准测试会随输入法一起演进。未来可以增加规模更大或分布不同的语料，但基准名称应包含案例数量或其他清晰后缀。每次公布结果时，都应记录引擎与词库版本、测试程序行为、延迟模式和评分方法，避免后续对比失去解释性。
